# Host-specific monitor + workspace-output config lives in hosts/<name>/.config/hypr/host.d/
# Empty glob (laptop without overlay) is a no-op — hyprland accepts zero-match globs.
source = ~/.config/hypr/host.d/*.conf

# Execute at launch
exec-once = ~/.config/hypr/xdg-portal-hyprland
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP DISPLAY XAUTHORITY
# Fix USB peripheral issues - proper seat management
exec-once = systemctl --user import-environment XDG_SESSION_TYPE XDG_SESSION_CLASS
exec-once = dbus-update-activation-environment --systemd --all
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = waybar
exec-once = nm-applet --indicator
exec-once = swaybg -m fill -i ~/.config/hypr/background.jpg

# Auto-start applications on specific workspaces
exec-once = [workspace 1 silent] alacritty
exec-once = [workspace 2 silent] alacritty -e nvim .
exec-once = [workspace 3 silent] brave
exec-once = [workspace 5 silent] thunderbird
exec-once = [workspace 4 silent] discord

# Input configuration
input {
    kb_layout = de
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 0  # Equivalent to i3's "focus_follows_mouse no"
    
    touchpad {
        natural_scroll = no
    }

    sensitivity = 0
}

general {
    gaps_in = 1
    gaps_out = 5
    border_size = 1
    col.active_border = rgb(bd93f9)  # Purple accent like i3 config
    col.inactive_border = rgba(6272a4aa)

    layout = dwindle
}

misc {
    disable_hyprland_logo = yes
    disable_splash_rendering = yes
    mouse_move_enables_dpms = true
    key_press_enables_dpms = true
}

decoration {
    rounding = 2
    
    blur { 
        enabled = true
        size = 7
        passes = 3
    }

    # drop_shadow = yes
    # shadow_range = 4
    # shadow_render_power = 3
    # col.shadow = rgba(1a1a1aee)
}

animations {
    enabled = yes

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    pseudotile = yes
    preserve_split = yes
}

gestures {
    # workspace_swipe = off
}

# Window rules - assign applications to workspaces
windowrulev2 = workspace 3, class:^(thorium|Thorium|brave|Brave|brave-browser)$
windowrulev2 = workspace 4, class:^(discord|Discord)$
windowrulev2 = workspace 5, class:^(mail|Mail|thunderbird|Thunderbird)$
windowrulev2 = workspace 6, class:^(heroic|Heroic)$
windowrulev2 = workspace 7, class:^(steam|Steam)$

# Transparency rules
windowrulev2 = opacity 0.95 0.95, class:^(Alacritty)$
windowrulev2 = opacity 0.9 0.9, class:^(thunar)$

# Variables
$mod = SUPER
$terminal = alacritty
$fileManager = thunar
$menu = wofi --show drun

# Key bindings

# Terminal
bind = $mod, Return, exec, $terminal
bind = $mod SHIFT, Return, exec, $terminal -e nvim .

# Kill focused window
bind = $mod SHIFT, Q, killactive

# Application launchers
bind = $mod, SPACE, exec, $menu
bind = $mod, B, exec, brave
bind = $mod, N, exec, upnote
bind = $mod, M, exec, thunderbird
bind = $mod, A, exec, pavucontrol
bind = $mod, E, exec, $fileManager # Show the graphical file browser
bind = $mod, D, exec, dmenu_run

# Screenshots (flameshot)
bind = $mod SHIFT, S, exec, flameshot gui

# Audio device switching
bind = $mod CTRL, N, exec, pactl set-default-sink $(pactl list short sinks | grep -i 'headset' | head -n1 | cut -f2) && notify-send 'Audio' 'Switched to Headset'
bind = $mod CTRL, S, exec, pactl set-default-sink $(pactl list short sinks | grep -i 'umc404hd' | head -n1 | cut -f2) && notify-send 'Audio' 'Switched to UMC404HD'

# Dark/Light mode toggle
bind = $mod SHIFT, B, exec, gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
bind = $mod SHIFT, M, exec, gsettings set org.gnome.desktop.interface color-scheme 'prefer-light'

# Volume control
bind = , XF86AudioRaiseVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ +10%
bind = , XF86AudioLowerVolume, exec, pactl set-sink-volume @DEFAULT_SINK@ -10%
bind = , XF86AudioMute, exec, pactl set-sink-mute @DEFAULT_SINK@ toggle
bind = , XF86AudioMicMute, exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle

# Window management - Focus (vim keys)
bind = $mod, H, movefocus, l
bind = $mod, J, movefocus, d
bind = $mod, K, movefocus, u
bind = $mod, L, movefocus, r

# Move windows (vim keys)
bind = $mod SHIFT, H, movewindow, l
bind = $mod SHIFT, J, movewindow, d
bind = $mod SHIFT, K, movewindow, u
bind = $mod SHIFT, L, movewindow, r

# Split orientation
bind = $mod, V, togglesplit  # vertical split
bind = $mod SHIFT, V, layoutmsg, preselect l  # horizontal split hint

# Fullscreen
bind = $mod, F, fullscreen

# Layout modes
bind = $mod, S, exec, hyprctl keyword general:layout master  # stacking-like
bind = $mod, W, togglegroup  # tabbed mode
bind = $mod, E, exec, hyprctl keyword general:layout dwindle  # toggle split

# Floating
bind = $mod SHIFT, Space, togglefloating
# bind = $mod, Space, focusmonitor, +1  # change focus between monitors

# Reload/restart
bind = $mod SHIFT, C, exec, hyprctl reload
bind = $mod SHIFT, R, exec, hyprctl reload

# Exit
bind = $mod SHIFT, E, exit

# Workspace switching
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

# Numpad workspace switching (Num Lock on)
bind = $mod, KP_End, workspace, 1      # KP_1
bind = $mod, KP_Down, workspace, 2     # KP_2
bind = $mod, KP_Next, workspace, 3     # KP_3
bind = $mod, KP_Left, workspace, 4     # KP_4
bind = $mod, KP_Begin, workspace, 5    # KP_5
bind = $mod, KP_Right, workspace, 6    # KP_6
bind = $mod, KP_Home, workspace, 7     # KP_7
bind = $mod, KP_Up, workspace, 8       # KP_8
bind = $mod, KP_Prior, workspace, 9    # KP_9
bind = $mod, KP_Insert, workspace, 10  # KP_0

# Move window to workspace
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Numpad move to workspace
bind = $mod SHIFT, KP_End, movetoworkspace, 1
bind = $mod SHIFT, KP_Down, movetoworkspace, 2
bind = $mod SHIFT, KP_Next, movetoworkspace, 3
bind = $mod SHIFT, KP_Left, movetoworkspace, 4
bind = $mod SHIFT, KP_Begin, movetoworkspace, 5
bind = $mod SHIFT, KP_Right, movetoworkspace, 6
bind = $mod SHIFT, KP_Home, movetoworkspace, 7
bind = $mod SHIFT, KP_Up, movetoworkspace, 8
bind = $mod SHIFT, KP_Prior, movetoworkspace, 9
bind = $mod SHIFT, KP_Insert, movetoworkspace, 10

# Mouse bindings
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# Disable mouse wheel workspace switching on waybar
# (This is typically configured in waybar config, not here)
